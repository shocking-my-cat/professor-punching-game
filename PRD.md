# 📄 PRD: '가상 교수 타격기 (Project: 빠른 종강)'
**프로젝트명:** 개강뻐ㅋ킹웹사이트

## 1. 프로젝트 개요
사용자가 직접 타겟(교수님)의 이름과 외모를 설정하고 생성된 캐릭터를 클릭하여 타격하는 인터랙티브 스트레스 해소 미니게임입니다. 
캐릭터의 체력(HP)을 0으로 만들면 가짜 '종강'을 맞이하게 되어 대학생들에게 쾌감을 선사합니다. 백엔드와 DB 없이 프론트엔드 단독으로 구동되는 B급 감성의 초경량 웹앱입니다.

## 2. 개발 제약 사항 (Out of Scope)
5일 컷 단기 속성 개발 및 휘발성 도파민 분출을 위해 다음 기능은 **절대 구현하지 않습니다**.
*   **로그인 X:** 사용자 인증 및 세션 관리 불필요. 접속 즉시 익명으로 진입.
*   **결과 저장 X:** 상태는 브라우저 메모리에만 존재하며 새로고침/종료 시 즉각 증발.
*   **모바일 최적화 X:** 데스크탑(PC) 환경의 마우스 광클 이벤트에만 맞춘 고정 레이아웃 사용.
*   **파일 업로드 X:** 초상권 리스크 방지를 위해 텍스트 입력과 AI(또는 에셋 조합) 묘사에만 의존.
*   **데이터베이스 X:** 백엔드 API 연동이나 DB 스키마 없이 100% 프론트엔드 단독 실행.

## 3. 핵심 유저 플로우
1.  **입력 1:** "당신을 괴롭히는 교수님의 이름을 알려주세요." (텍스트 입력)
2.  **입력 2:** "교수님은 어떻게 생기셨나요? 50자 이내로 적어주세요." (텍스트 입력)
3.  **캐릭터 렌더링:** 묘사를 바탕으로 교수 캐릭터와 촌스러운 이름표 생성.
4.  **타격 게임 시작:** 화면 정중앙에 배치된 교수 캐릭터를 마우스로 미친 듯이 클릭 (HP 감소).
5.  **종강(엔딩):** 체력이 0이 되면 화려한 이펙트와 함께 종강 메시지 출력.

## 4. 핵심 상태 (State) 정의
| State 명칭 | 타입 | 초기값 | 설명 |
| :--- | :--- | :--- | :--- |
| `step` | String | 'input' | 현재 화면 상태 ('input', 'playing', 'end') |
| `profName` | String | '' | 교수 이름 (최대 10자 제한) |
| `profDesc` | String | '' | 교수 외모 묘사 (최대 50자 제한) |
| `hp` | Number | 1000 | 교수 캐릭터의 체력 |
| `clicks` | Array | [] | 화면에 표시될 데미지 텍스트(`-10`)의 좌표 배열 |

## 5. 화면별 상세 기능 요구사항

### View 1: 입력 화면 (step: 'input')
*   **레이아웃:** 화면 중앙에 큼직하고 투박한 입력 폼 박스 배치. (B급 감성, 붉은색/검은색 대비)
*   **입력 필드 1 (이름):** 텍스트 input, `maxLength={10}`. 특수문자 제외.
*   **입력 필드 2 (외모):** 텍스트 textarea, `maxLength={50}`. 50자 초과 시 입력 차단 유효성 검사.
*   **버튼:** "교수님 소환하기" 큼직한 버튼. 클릭 시 `step` -> 'playing'.

### View 2: 게임 화면 (step: 'playing')
*   **체력바 (HP Bar):** 화면 최상단에 너비 80%를 차지하는 거대하고 붉은 프로그레스 바. 초기 체력 1000.
*   **캐릭터 렌더링:** 중앙에 교수 캐릭터 아이콘/이미지 배치. 머리 위에 `profName`이 적힌 명찰 고정.
*   **타격 인터랙션 (매우 중요):**
    *   캐릭터(hitbox) 클릭 시 `hp` 10 감소.
    *   클릭 시 캐릭터에 0.1초간 좌우 흔들림(Shake) 및 붉은색 필터 효과.
    *   마우스 좌표를 추출하여 클릭 위치에 붉고 굵은 폰트로 **`-10`** 동적 생성.
    *   `-10` 텍스트는 1초 동안 위로 이동(Y축)하며 투명해지는(Fade-out) 애니메이션 후 DOM에서 삭제.
*   **종료 조건:** `hp <= 0` 달성 시 `step` -> 'end' 즉시 전환.

### View 3: 종료 화면 (step: 'end')
*   **화면 전환:** 이전 화면 캐릭터가 깨지거나 폭발하는 연출.
*   **텍스트 UI:** 화면 정중앙에 궁서체로 거대하게 **"🎉 당신은 종강했습니다! 🎉"** 출력.
*   **배경 이펙트:** 화면 전체에 흩날리는 꽃가루(Confetti) 이펙트 무한 재생.
*   **버튼:** "다시 개강하기(초기화)" 버튼 클릭 시 모든 State 초기화.